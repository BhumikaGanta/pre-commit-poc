## Pre-commit quality checks

- [ ] I ran `pre-commit run --all-files` locally, or reviewed the CI pre-commit result.
- [ ] Formatting, linting, and whitespace fixes are handled by automation rather than review comments.
- [ ] If CI failed, I checked the GitHub Actions summary for hook runtime and failing OS.
